{
  "name": "seneca-browser",
  "version": "0.10.0",
  "description": "Seneca for the browser.",
  "main": "seneca-browser.js",
  "scripts": {
    "test": "lab -P test -I jsonic,patrun,fetch",
    "prettier": "prettier --write --no-semi --single-quote seneca-browser-src.js test/*.js",
    "browserify": "browserify -o seneca-browser.js -e seneca-browser-src.js -s Seneca",
    "build": "npm run browserify",
    "smoke": "node smoke/server.js",
    "reset": "npm run clean && npm i && npm test",
    "clean": "rm -rf node_modules package-lock.json yarn.lock",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag v$REPO_VERSION && git push --tags",
    "repo-publish": "npm run clean && npm i --registry http://registry.npmjs.org && npm run prettier && npm run build && npm test && npm run repo-tag && npm publish --access public --registry http://registry.npmjs.org"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/voxgig/seneca-browser.git"
  },
  "author": "Richard Rodger",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/voxgig/seneca-browser/issues"
  },
  "homepage": "https://github.com/voxgig/seneca-browser#readme",
  "dependencies": {},
  "devDependencies": {
    "@hapi/code": "^8.0.1",
    "@hapi/hapi": "^18.4.0",
    "@hapi/inert": "^5.2.2",
    "@hapi/joi": "^17.1.0",
    "@hapi/lab": "^22.0.3",
    "@seneca/external": "^1.1.0",
    "@seneca/hapi": "^1.0.0",
    "browserify": "^16.5.0",
    "browserify-fs": "^1.0.0",
    "browserify-header": "^1.0.1",
    "prettier": "^1.19.1",
    "seneca": "^3.19.0",
    "seneca-promisify": "^1.2.0",
    "timers": "^0.1.1"
  },
  "files": [
    "README.md",
    "LICENSE",
    "seneca-browser.js"
  ]
}
